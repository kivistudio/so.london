---
interface Props {
  current?: 'about' | 'notes' | 'inspirations' | null;
}
const { current = null } = Astro.props;
---

<nav class="nav">
  <a href="/about/" class="nav__link" aria-current={current === 'about' ? 'page' : null}>
    About
  </a>
  <a href="/notes/" class="nav__link" aria-current={current === 'notes' ? 'page' : null}>
    Notes
  </a>
  <a href="/inspirations/" class="nav__link" aria-current={current === 'inspirations' ? 'page' : null}>
    Inspirations
  </a>
</nav>

<style>
  .nav {
    position: fixed;
    top: var(--page-pad-y);
    right: var(--page-pad-x);
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: var(--s-2);
    z-index: 10;
  }

  .nav__link {
    font-family: var(--font-mono);
    font-size: var(--nav-link-size);
    letter-spacing: var(--nav-tracking);
    text-transform: uppercase;
    color: var(--fg);
    padding: var(--s-1) var(--s-2);
    transition: opacity 160ms ease;
  }

  .nav__link:hover,
  .nav__link:focus-visible {
    opacity: 0.7;
  }

  .nav__link[aria-current='page'] {
    border-bottom: 1px solid var(--hairline);
  }
</style>
